// Get a key from https://console.anthropic.com/settings/keys
// ⚠️ This embeds the key inside the compiled app, so anyone who decompiles
// the APK can read it. Fine for a personal/private build; for a public app
// you'd instead call your own small backend that holds the key server-side.
export const ANTHROPIC_API_KEY = "PASTE_YOUR_ANTHROPIC_API_KEY_HERE";
